@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Welcome</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
</div>
@* TAG HELPER ???*@
@* <form asp-controller="order" method="post">
    <legend>CRUSS</legend>
    @foreach (var item in Model.Sizes)
    {
    <input type="radio" asp-for="@Model.SelectedSize" value="@item">
    <label asp-for="@item">@item</label>
    @Html.ValidationMessageFor(m => m.SelectedSize)
    }<br>
    </form>
    <form asp-controller="order" method="post">
    <legend>SIEZ</legend>
    @foreach (var item in Model.Crusts)
    {
    <input type="radio" asp-for="@Model.SelectedCrust" value="@item">
    <label asp-for="@item">@item</label>
    @Html.ValidationMessageFor(m => m.SelectedCrust)
    }<br>
    </form> *@
@model PizzaBox.Client.Models.OrderViewModel

<body>
    @Html.AntiForgeryToken()
    <form action="/order" method="post" novalidate="novalidate">
        <fieldset>
            <legend>CRUSS</legend>
            @Html.ValidationMessageFor(m => m.SelectedSize)
            @foreach (var item in Model.Sizes)
            {
                <input type="radio" id="@item" name="selectedcrust" value="@item">
                <label for="@item">@item</label>
                <br>
            }
        </fieldset>
        <fieldset>
            <legend>SIEZ</legend>
            @Html.ValidationMessageFor(m => m.SelectedCrust)
            @foreach (var item in Model.Crusts)
            {
                <input type="radio" id="@item" name="selectedsize" value="@item">
                <label for="@item">@item</label>
                <br>
            }
        </fieldset>
        <fieldset>
            <legend>TOPPERS</legend>
            @Html.ValidationMessageFor(m => m.SelectedToppings)
            @foreach (var item in Model.Toppings)
            {
                <input type="checkbox" id="@item" name="selectedtopping" value="@item">
                <label for="@item">@item</label>
                <br>
            }
        </fieldset>

        <button type="submit">SUMBIT ORDERR</button>
    </form>
</body>